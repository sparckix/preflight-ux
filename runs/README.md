# Runs

Panel runs store model provenance, prompt versions, persona IDs, normalized
finding records, and issue classes. Public runs keep redacted execution
receipts. Full raw model transcripts belong in ignored
`internal/archive/raw-model-outputs/`.

Current public runs are benchmark or baseline artifacts. Keep exploratory,
failed, or superseded runs in ignored `internal/archive/` rather than in this
directory.
